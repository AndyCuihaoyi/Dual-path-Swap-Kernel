Linux kernel (5.10.43, Dual-path Swap)
======================================

以下为在本树中进行的「双路径 Swap」定制说明，便于在不同工作环境间对齐背景与进度；其余仍为上游内核说明。

Dual-path Swap 项目概述
-----------------------

动机：在手机等 UFS/NAND 高带宽但随机 I/O 代价高的场景中，匿名页若始终沿「RAM → 压缩缓存 → 块设备」单路径、以单页为主换出，容易放大中间层与随机写的问题。本方案将回收分为两条路径：

- 路径 A（离散单页）：未形成强共现关系的冷匿名页，继续走传统 inactive anon LRU，并复用 Zswap 等压缩缓存，用 CPU 换空间、减轻随机落盘。
- 路径 B（聚合页组）：对高共现匿名页组成 Page Group，在 Swap 上按 Multi-Order 分配连续槽位，整组顺序 I/O 下刷（绕开压缩），换入时按 Order 块聚合读，并对填充碎片槽位的离散页做隔离/高优先级回收，避免预取污染。

配套机制包括：基于时间窗与页表 Accessed/Young 位的轻量采集、Transaction ID 环形缓冲、指纹相似度与分桶挖掘、专用线程（设计稿中为 kaggswapd）与现有 kswapd 分工等。详细设计与实验数据见仓库内 ``.cursor/context/`` 下可行性报告等文档；编码约定见 ``.cursorrules``。

当前代码进度（同步点，2026-05 延续）
---------------------------------

已具备（编译与数据骨架）：

- Kconfig：``CONFIG_DUAL_PATH_SWAP``（``mm/Kconfig``），依赖 ``ZSWAP``，并 ``select PAGE_EXTENSION``；帮助文字说明了运行时可由 sysctl/启动参数关闭及扩展开销来源。
- 每页扩展：``PAGE_EXTENSION`` tail 中为 ``struct page_ext_agg``，**当前实现仅放一个指针**
  ``struct page_ext_agg_data *data``（``include/linux/page_ext.h``）。真正记账结构
  ``struct page_ext_agg_data``（``window_ids[]``、``head``、``last_kagg_win``、``group``、
  ``miner_node``）
  在 **首次采样需要写窗** 时：头文件内联 ``page_ext_agg_ensure_data()`` 先用 ``READ_ONCE`` 快路径，
  未命中则 ``__page_ext_agg_ensure_slow()`` 以 ``GFP_NOWAIT | __GFP_NOWARN`` 从 kmem_cache
  ``page_ext_agg_data`` 取零对象并以 ``cmpxchg`` **无锁挂载**；未盖章页 ``data`` 为空。
  PFN 归还时在 ``mm/page_alloc.c`` 的 ``free_pages_prepare()``（``reset_page_owner`` 之后）
  调用 ``dual_path_page_ext_prepare_free()``，对 ``page + i`` 做 ``lookup_page_ext_agg``、``xchg`` 清指针并 ``kmem_cache_free``，避免 PFN 复用脏数据泄漏（``mm/page_ext.c``）。
  ``page_ext_agg_ops`` 的 ``size`` 现为 ``sizeof(struct page_ext_agg)``（指针大小）。
  挂上 ``group`` 后须在释放路径前摘除，否则 ``prepare_free`` 会 ``WARN_ON_ONCE``。
- 聚合 LRU：``struct lruvec`` 增加 ``agg_list``；``struct page_group`` 及 ``lruvec_page_group_*`` 链表操作（``include/linux/mmzone.h``、``mm/mmzone.c``）；需在持 ``lru_lock`` 下操作（见注释与 ``lockdep_assert_held``）。
- 运行时总开关：本树默认 **开启** Dual-path（``sysctl_dual_path_swap`` 初值为 1，见 ``mm/swap.c``）；仍可通过启动参数 ``dual_path_swap=0`` 或 ``sysctl -w vm.dual_path_swap=0`` 关闭。``include/linux/swap.h`` 中 ``dual_path_swap_enabled()`` 供各路径判断（未开启 CONFIG 时恒为 false）。
- 聚合线程与采样（可按配置裁掉）：``CONFIG_KAGGSWAPD`` 对应 ``mm/kaggswapd.c`` / ``include/linux/kaggswapd.h``；内核线程名为 ``kaggswapd``，通过 ``vm.dual_path_scan_mode``、``dual_path_scan_pct``、``dual_path_scan_interval_ms`` 调度多种 LRU 扫描策略并把窗口 ID 写入 **按需存在的** ``page_ext_agg_data``（经 ``lookup_page_ext_agg`` + ``ensure_data``）。
- Miner 周期器：``vm.dual_path_miner_enable=1`` 时按 ``vm.dual_path_miner_interval_ms`` 触发；对每个在线 NUMA node、再对每个在线 ``mem_cgroup`` 的 ``lruvec``，从 ``LRU_INACTIVE_ANON`` 按 ``vm.dual_path_scan_pct`` 比例确定“视野”上限（受 ``KAGG_MINER_VIEW_MAX`` 保护），随后以 ``KAGG_MINER_BATCH_MAX`` 小批次循环执行“隔离 -> 分桶”并在桶中累积候选页（侵入式 ``miner_node``），最后按 ``vm.dual_path_miner_similarity_pct`` 做贪心聚类，达到 ``vm.dual_path_miner_min_cluster`` 的簇生成 ``page_group`` 元数据挂入 **该 ``lruvec``** 的 ``agg_list``，并统一 ``putback_lru_page``。挖掘 LRU 游标按 **``memcg_id`` + node** 记录在 ``struct kagg_memcg_meta::cursor_lru_steps[]``（见 ``mm/kaggswapd.c``），避免不同 cgroup 共享同一 node 游标。
- **per-memcg 元数据生命周期**：``kagg_memcg_meta_get()`` 对非根 ``memcg_id`` 在哈希表中 ``kzalloc`` 的 ``struct kagg_memcg_meta``，在 ``mm/memcontrol.c`` 的 ``mem_cgroup_css_offline()`` 中、``mem_cgroup_id_put()`` **之前**调用 ``kagg_memcg_meta_free()``（``include/linux/kaggswapd.h`` 声明）摘除节点并 ``kfree``，避免 Docker/K8s 场景下 **memcg_id 复用** 与 **slab 泄漏**；根 memcg（id 0）仍使用静态 ``kagg_root_meta``，不经过该释放路径。
- **相似度计算微结构**：``kagg_windows_unique()`` 对环形窗内去重后的 ``u16`` 窗口 ID 做插入排序；``kagg_miner_similar()`` 对两有序序列用 **双指针** 求交集，单次比较上界 ``O(MAX_PAGE_EXT_AGG_WINDOW)`` 量级（``MAX_PAGE_EXT_AGG_WINDOW=16``）。
- **VMA 展开采样**：``kagg_scan_inactive_vma_expand`` 在将 seed 压入 ``kagg_vma_seed_buf`` 前增加 ``page_mapped(page)`` 判断，跳过已无 PTE 映射的页，减少 ``page_get_anon_vma`` / ``walk_page_vma`` 空转。
- 调试统计：``CONFIG_KAGGSWAPD_DEBUG``（``mm/Kconfig``，本树 **默认 y**）在 ``debugfs`` 下提供 ``kaggswapd/summary``；依赖 ``CONFIG_DEBUG_FS``。详见下文「调试方式」。

尚未接入或待实现（与方案文档对应的主线）：

- 定时器驱动的页表遍历（与当前 LRU 采样式 ``kagg_*`` API 并行存在时的策略取舍）。
- Pattern Miner：已接入“隔离+哈希分桶+桶内贪心相似度聚类+page_group 元数据入 ``agg_list``+回填”；仍待接入按页成员链维护、下刷队列与 Swap I/O 执行路径（计划继续落在 ``kaggswapd.c`` / reclaim 主路径）。
- ``mm/vmscan.c`` / Swap 路径：在 ``dual_path_swap_enabled()`` 下对离散路径与聚合路径的分流，以及与 Zswap、底层 swap 设备的协同。
- Multi-Order Swap 簇分配器、聚合路径与 Swap 栈深度对接，以及聚合换入/预取与「防污染」页状态策略。
- 测试与文档：自测用例、``Documentation/`` 或模块说明（若需对外维护）。

扫描语义分层（采样 vs 挖掘）
----------------------------

- 采样扫描（Sampling / Stamping）：目标是写 ``window_ids`` 与 ``last_kagg_win``，产出访问特征。
  - 采样-LRU：逐页扫描并打标记。
  - 采样-VMA：从 Inactive anon 尾部按 ``vm.dual_path_scan_pct`` 比例取 seed（带上限保护）；仅 ``page_mapped`` 的 seed 入队，经 RMAP 找 VMA 并 ``walk_page_vma``，对关联匿名页打标。
- 挖掘扫描（Mining / Clustering）：目标是将高相似度页面成团（``page_group``）。
  - 挖掘-LRU：批次隔离页面，分桶并聚类，满足阈值后生成 ``page_group`` 元数据并挂 ``agg_list``。
  - 挖掘-VMA：以 seed 为中心，通过 RMAP 拉取 VMA 匿名页并做相似度比对（待完整接入）。

运行时 sysctl 与启动参数（快速参考）
------------------------------------

前提：内核需 ``CONFIG_DUAL_PATH_SWAP=y``；采样相关项还需 ``CONFIG_KAGGSWAPD=y``。节点位于 ``/proc/sys/vm/``。

- ``vm.dual_path_swap``：**本树默认 1**，取值 0 或 1；总开关。为 0 时 ``kaggswapd`` 不推进扫描，仅在低功耗轮询中睡眠等待。
- ``vm.dual_path_scan_mode``：**默认 0**，合法范围 0–3：0 Inactive anon 前 ``pct%``；1 Active 前 ``pct%``；2 全 inactive + active anon（忽略 ``pct``）；3 从 Inactive anon 尾部按 ``pct%`` 取 seed，经 RMAP 与 VMA 页表遍历展开。
- ``vm.dual_path_scan_pct``：**默认 30**，合法 1–100；用于 mode 0、1 的 bounded 扫描比例，mode 3 的 seed 选取比例，以及 miner 的“每轮视野目标”比例（最终受 ``KAGG_MINER_VIEW_MAX`` 与 ``KAGG_MINER_BATCH_MAX`` 的分层上限约束）。
- ``vm.dual_path_scan_interval_ms``：**默认 100**，合法 10–600000；每轮「所有在线 NUMA node 采样」结束后的休眠毫秒数。
- ``vm.dual_path_miner_enable``：**默认 0**（见 ``mm/kaggswapd.c`` 初值；可通过 sysctl 置 1），合法 0–1；为 1 时按 ``dual_path_miner_interval_ms`` 周期执行挖掘平面（仍受 ``dual_path_swap`` 总开关约束）。
- ``vm.dual_path_miner_interval_ms``：**默认 6000**，合法 1000–36000（见 ``kernel/sysctl.c`` 与 ``kaggswapd_fn`` 内 clamp）；挖掘块之间的最小间隔（毫秒）。
- ``vm.dual_path_miner_similarity_pct``：**默认 75**，合法 50–100（见 ``kernel/sysctl.c``）；桶内 ``kagg_miner_similar`` 判定阈值（百分比，交集相对较大集合）。``kagg_miner_cluster_and_putback`` 内另将读值 clamp 至 1–100。
- ``vm.dual_path_miner_min_cluster``：**默认 33**，合法 33–4096（见 ``kernel/sysctl.c``）；单簇至少页数，低于则不成团并走回填。聚类路径内另 clamp 至 1–``KAGG_MINER_CLUSTER_MAX``。

常用命令示例（查看与写入）::

	sysctl vm.dual_path_swap vm.dual_path_scan_mode vm.dual_path_scan_pct vm.dual_path_scan_interval_ms vm.dual_path_miner_enable vm.dual_path_miner_interval_ms vm.dual_path_miner_similarity_pct vm.dual_path_miner_min_cluster
	sudo sysctl -w vm.dual_path_swap=0
	sudo sysctl -w vm.dual_path_swap=1

内核命令行可早于用户态覆盖默认开启，例如 ``dual_path_swap=0``。解析实现在 ``mm/swap.c`` 的 ``dual_path_swap_setup()``。

调试方式（``CONFIG_KAGGSWAPD_DEBUG``）
-------------------------------------

本树在 ``mm/Kconfig`` 中将其 **默认为 y**。须 ``CONFIG_DEBUG_FS=y``。

挂载 debugfs（若尚未挂载）::

	sudo mount -t debugfs none /sys/kernel/debug

在存在一定匿名负载且 ``vm.dual_path_swap=1`` 时，读取调试汇总（字段以 ``mm/kaggswapd.c`` 中 ``kagg_summary_show`` 为准）::

	sudo cat /sys/kernel/debug/kaggswapd/summary

``summary`` 中主要字段：

- ``=== 系统概况 ===``：全局内存与 **memcg**：``memcg_summary(total/online/hierarchy/max_depth)`` 一行汇总（``memcg_total`` / ``memcg_online`` / ``use_hierarchy`` / ``memcg_max_depth``）。随后 ``memcg_top10``：一行英文说明（``group_size`` 列对应 ``clusters_emitted``、``clustered_pages`` 对应 ``cluster_pages``、无 per-memcg meta 时后三项为 0）。与 ``memcg_summary`` 计数共用 **一次** ``mem_cgroup_iter`` 全树遍历，边遍历边将各行写入按需倍增的 ``kvmalloc`` 表；再按 ``group_size`` **降序** 排序（``group_size`` 相同则 ``clustered_pages`` 降序，再按 ``memcg_id`` 升序），输出 **前 10** 行；表分配失败或中途失败时打印 ``memcg_top10(alloc_failed or incomplete…)``（若已有部分行仍会排序输出）。每行形如 ``memcg_id=…``（**不含** cgroup 路径名）。**不再**输出整棵 ``memcg_tree``。
- ``scan_rounds`` / ``pages_stamped`` / ``dedup_same_window``：采样侧累计。
- ``=== 挖掘核心统计 ===``：``miner_rounds``、``miner_isolated_pages``、``miner_view_target_pages``。**不再**在 ``summary`` 中打印 chunk 级 ``miner_iso_tail`` / ``miner_iso_quota``；隔离凑批的调试计数（``kagg_dbg_miner_iso_*``）仍在代码路径中累加，供后续扩展 trace 或单独导出。
- ``=== 聚类结果 ===``：``miner_clusters_emitted``、``miner_cluster_pages``、``miner_putback_pages``、``miner_groups_cleared``、``miner_members_linked``、``miner_bucket_trunc`` 等。
- ``=== 聚合组概览 ===``：跨 node、跨 memcg 的 ``agg_list`` 汇总（best-effort）。
- ``next_win_id``：全局 u16 窗口号的无锁窥探，可能与实际略有竞态。

**采样打标路径注意**：``kagg_try_mark_window`` 侧对 ``pfn_valid``、``READ_ONCE(agg->data)==d`` 及 ``page_ext_agg_push_window_into`` 中 ``head`` 钳位等防护，见「问题记录」2b；``kagg_scan_lru_bounded`` 在 ``get_page`` 成功后 ``smp_mb()`` 再释放 ``lru_lock``，减轻与 ``dual_path_page_ext_prepare_free`` 的乱序竞态。

**不再**在 ``summary`` 中逐 memcg 打印完整挖掘列表；分桶截断与 memcg 粒度的量化分析见「问题记录」第 3 条。

若需去掉盖章路径上的计数与环形缓冲区开销，在配置中将 ``CONFIG_KAGGSWAPD_DEBUG`` 置为 ``n``（``make menuconfig`` → ``Memory Management options`` → ``kaggswapd debug statistics (debugfs)``）。

构建提示：启用 ``ZSWAP``、``DUAL_PATH_SWAP``；采样线程默认 ``CONFIG_KAGGSWAPD=y``。整编并启动本树内核后，**无需再手动 sysctl 打开** 即可执行聚合采样路径（除非你用启动参数或 sysctl 关掉总开关）。

参数快速下发：仓库根目录提供 ``kaggswapd_tune.sh``，用于开机后一次性写入关键 ``vm.dual_path_*`` 参数并读取 ``debugfs`` 汇总，便于复现实验配置与回归对比。


------------------------------------------------------------------------

There are several guides for kernel developers and users. These guides can
be rendered in a number of formats, like HTML and PDF. Please read
Documentation/admin-guide/README.rst first.

In order to build the documentation, use ``make htmldocs`` or
``make pdfdocs``.  The formatted documentation can also be read online at:

    https://www.kernel.org/doc/html/latest/

There are various text files in the Documentation/ subdirectory,
several of them using the Restructured Text markup notation.

Please read the Documentation/process/changes.rst file, as it contains the
requirements for building and running the kernel, and information about
the problems which may result by upgrading your kernel.


------------------------------------------------------------------------

问题记录

0. （2026-05 更新）**page_ext_agg 懒分配与释放**：tail 仅存指针槽；采样首次写窗 kmem_cache_zalloc；
   页面释放在 ``free_pages_prepare`` 中 ``kmem_cache_free``；快路径在内联 ``page_ext_agg_ensure_data()`` 中为 ``READ_ONCE`` 命中，
   ``GFP_NOWAIT`` 慢路径挂载使用 ``cmpxchg``。

1. 下一步我们要对匿名页进行挖掘，单独设置一个计时器进行周期性挖掘（这个周期需要远大于扫描周期），进行挖掘时理论上扫描的页面和我们采样时扫描的页面是一致的，但是由于两个动作的周期不一致，比如说我们打标记的页会晋升或者降级；所以除了每次都扫描全局匿名页的模式，我们在管理生命周期的方法上还需要考虑一下。

2. （2026-05 稳定性排障）miner enable 后出现 RCU stall 的根因是：早期实现在持有 ``pgdat->lru_lock`` 时调用了 ``isolate_lru_page()``。该接口在 ``mm/vmscan.c`` 的约束明确要求调用方不得持有 ``lru_lock``（其内部会自行加锁），违背调用契约会导致长时间锁阻塞与调度停滞，最终触发 ``rcu_sched detected stalls``。修复原则：锁内仅做轻量遍历/取引用，隔离与重计算放到锁外执行，并配合分段 ``cond_resched()``。

2b. （2026-05 稳定性）采样路径 ``kagg_scan_lru_bounded`` 在 ``get_page_unless_zero`` 之后释放 ``lru_lock`` 再调用 ``kagg_try_mark_window``；若与 ``dual_path_page_ext_prepare_free`` 存在极端竞态，可能出现对已释放 ``page_ext_agg_data`` 的解引用（``kagg_try_mark_window+0x54`` 类页故障）。缓解：``pfn_valid`` 门禁、``READ_ONCE(agg->data)==d`` 在访问 ``d`` 前后校验、``smp_mb()`` 保证引用计数在解锁前可见；环写入口对 ``head`` 做越界钳位（``page_ext_agg_push_window_into``）。

2c. （2026-05 生产排雷）**per-memcg 元数据泄漏**：``kagg_memcg_meta_get`` 对非根 ``memcg_id`` 在 ``kagg_memcg_meta_hash`` 中 ``kzalloc`` 的 ``struct kagg_memcg_meta`` 原先无释放路径。现于 ``mm/memcontrol.c`` 的 ``mem_cgroup_css_offline()`` 中、在 ``mem_cgroup_id_put()`` **之前**调用 ``kagg_memcg_meta_free()``（``mm/kaggswapd.c`` / ``include/linux/kaggswapd.h``），摘除哈希节点并 ``kfree``，避免容器频繁创建/销毁时 ``memcg_id`` 复用与 slab 泄漏。说明：挖掘线程仅遍历 ``mem_cgroup_online`` 的 memcg，与 ``css_offline`` 的极窄竞态若需绝对消除，可后续为 meta 增加引用计数或 RCU 释放。

2d. （2026-05 微优化）``kagg_windows_unique`` 对窗口 ID 做插入排序后，``kagg_miner_similar`` 用双指针求有序数组交集，将单次比较从 ``O(ns*nc)`` 降为 ``O(ns+nc)``（``MAX_PAGE_EXT_AGG_WINDOW=16`` 时上界 32）。VMA 展开路径 ``kagg_scan_inactive_vma_expand`` 在入 seed 缓冲前跳过 ``!page_mapped(page)``，避免对已无 PTE 映射的冷页做 ``page_get_anon_vma``/walk 空转。

3. （2026-05 观测）**memcg 粒度与「分桶截断」指标（``miner_bucket_trunc``）**

   **背景**：启用 cgroup 后，匿名页主要挂在各 ``mem_cgroup`` 的 ``lruvec`` 上；采样与挖掘已改为按 **每个在线 memcg × 每个在线 node** 的 ``lruvec`` 遍历（见 ``mm/kaggswapd.c`` 中 ``kagg_sample_scan_pgdat`` / ``kagg_miner_scan_pgdat``）。挖掘游标与部分累计统计按 **memcg_id** 单独维护（``struct kagg_memcg_meta``），避免不同 cgroup 共享同一 node 游标；**memcg 离线时** 由 ``kagg_memcg_meta_free()`` 回收对应 meta（见「问题记录」2c）。

   **``miner_bucket_trunc`` 含义**：在 ``kagg_miner_cluster_and_putback`` 中，对每个哈希桶做桶内贪心聚类时，若单桶内 **seed 轮数** 超过 ``KAGG_MINER_BUCKET_SEED_MAX``（本树为 **8192**），则对该桶调用 ``kagg_miner_bucket_strip_nodes`` 并 **累计 +1**。这是 **「每桶一次触顶」** 的计数，**不是页数**；一轮挖掘内最多 **1024** 个桶（``KAGG_MINER_HASH_SIZE``），故单 memcg 单轮理论上界为 1024 次截断（实际通常远小于该值）。

   **一次典型现场量化（单 NUMA、``dual_path_scan_pct=30``、miner 相似度 90%、``min_cluster=33``，``debugfs/kaggswapd/summary`` 快照）**：

   - **系统**：``numa_online_nodes=1``；``mem_anon_pages=2387``（active 182 / inactive 2205）；``memcg_total=67``，``memcg_online=39``。
   - **采样**：``scan_rounds=973``，``pages_stamped=1650007``。
   - **挖掘（全局汇总）**：``miner_rounds=20``；``miner_isolated_pages=24091``；``miner_view_target_pages=24112``（与 ``summary`` 当前字段一致；chunk 级 ``miner_iso_*`` 已不在 ``summary`` 中打印）。
   - **聚类（全局）**：``miner_clusters_emitted=139``；``miner_cluster_pages=12311``；``miner_putback_pages=11651``；**成团率** ``12311/24091≈51.1%``，**回填率** ``11651/24091≈48.4%``；``miner_bucket_trunc=893``（相对 20 轮全局挖掘，约 **44.7 次/轮** 的桶触顶 strip 累计，分散在多个 memcg 与多桶上）。
   - **单 memcg 切片示例（原 summary 曾逐行打印，现已迁至本文档留存）**：``memcg_id=1`` 在同期快照中为 ``rounds=21``、``isolated=1283``、``clustered=777``、``putback=506``、``bucket_trunc=47``。其中 **47 次截断 / 21 次该 memcg 挖掘计数 ≈ 2.24 次/计数周期**，表示平均每个周期约有 **2～3 个哈希桶** 触顶，**不**等价于「21 轮里失败 47 次」或「每轮必失败」。

   **解读与后续**：高 ``bucket_trunc`` 常与 **高 ``similarity_pct`` + 大 ``min_cluster`` + 分桶指纹较粗** 叠加有关，表现为桶内反复 seed / rebucket 仍难凑满簇；若同时 **回填高、成团率低**，可优先调参或改进分桶/提前打散策略。``debugfs/summary`` 仅保留 **全局** ``miner_bucket_trunc`` 等字段；**不再逐 memcg 打印** 上述切片（避免刷屏与锁内遍历成本），详细分 memcg 数据以实验日志或本文档记录为准。

   **口径说明**：全局 ``miner_rounds`` 按「整次 miner 块」递增；per-memcg 的 ``rounds`` 按「该 memcg 每 node 每轮一次 ``kagg_miner_scan_lruvec``」递增，在 **多 node** 或计数边界下可能与全局数字略有偏差，对比时以语义为准。
